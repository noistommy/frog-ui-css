const base = `
<div class="ga-message">
    <!-- contents -->
</div>
`

const state = `
<div class="ga-message { status }">
    <!-- contents -->
</div>
`

const stateText = `
<div class="ga-message { status }-text">
    <!-- contents -->
</div>
`

const stateBackground = `
<div class="ga-message { status }-bg">
    <!-- contents -->
</div>
`

const stateBorder = `
<div class="ga-message { status }-border">
    <!-- contents -->
</div>
`

module.exports = {
  base,
  state,
  stateText,
  stateBackground,
  stateBorder,
}
